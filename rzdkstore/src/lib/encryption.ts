/**
 * Encryption utilities for sensitive data in database.
 * Digunakan untuk enkripsi: password akun Netflix/ChatGPT, stok digital, info fulfillment.
 *
 * Menggunakan AES-256-GCM dengan key dari environment variable ENCRYPTION_KEY.
 * Akan diimplementasi lengkap di Fase 4.
 */

// Placeholder - akan diisi di Fase 4
export function encrypt(_text: string): string {
  // TODO: Implement AES-256-GCM encryption
  return _text;
}

export function decrypt(_encryptedText: string): string {
  // TODO: Implement AES-256-GCM decryption
  return _encryptedText;
}
