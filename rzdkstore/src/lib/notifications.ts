/**
 * Notification Dispatcher
 * Akan diimplementasi lengkap di Fase 10
 *
 * Logic:
 * 1. Selalu simpan ke tabel notifications (in-app)
 * 2. Coba kirim push notification jika user subscribe
 * 3. Fallback ke email jika push gagal atau user belum subscribe
 */

// Placeholder - akan diisi di Fase 10
export async function sendNotification(
  _userId: string,
  _payload: { title: string; message: string; type: string }
) {
  // TODO: Implement di Fase 10
}
